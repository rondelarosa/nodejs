// it's installs in global what does this it automatically executes
// no nodemo in node_modules as iti s global
// nodemon ./cli-package-demo.js

// nodemon automatically exuecutes when changes occurs
const _ = require('lodash');

console.log(_.random(1, 100));
